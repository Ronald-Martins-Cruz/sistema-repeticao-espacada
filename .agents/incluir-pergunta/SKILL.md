---
name: incluir-pergunta
description: Protocolo para adicionar pergunta(s) nova(s) ao banco de estudo em estudo.db. Use quando o usuário quiser incluir, criar ou cadastrar uma pergunta nova (ou várias), vincular uma pergunta a uma prova real, ou desativar uma pergunta existente.
---

# incluir-pergunta — ponteiro

Este arquivo não tem conteúdo próprio. O protocolo completo vive em
**`.claude/skills/incluir-pergunta/SKILL.md`** — leia esse arquivo antes de fazer
qualquer coisa com esta skill.

Só o `description` acima é duplicado, e de propósito: é o que torna a skill
descobrível por ferramentas que seguem a convenção `.agents/`. Todo o resto —
regras, passos, SQL — se edita no arquivo do `.claude/`, nunca aqui.
