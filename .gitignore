# O banco em si nao vai para o git: binario incha o repositorio a cada commit.
# O backup versionado e estudo.sql (dump textual, diffavel).
# Restaurar:  sqlite3 estudo.db < estudo.sql
estudo.db-wal
estudo.db-shm

__pycache__/
*.pyc

# Os PDFs de leitura sao regeraveis a partir do markdown:  python -m scripts.mdpdf gerar
# O que precisa ir para o git e scripts/mdpdf/marcacoes/ (o que voce grifou),
# nao o pdf/.
pdf/
